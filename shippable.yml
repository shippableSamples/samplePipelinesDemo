language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=harryi3t/dv
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "docker-test"
      type: docker
      branches:
        only:
          - master
